# Godot 配置说明

## 如何设计游戏显示窗口的大小？

一般来说，我们会将游戏显示窗口的宽高按照 16 ：9 的比例进行设置，从而，满足绝大多数的电脑显示器。常用的尺寸有`1920×1080，1280×720`。

拉伸模式设为 `canvas_items`（表示对象将根据其父级的大小进行拉伸。这意味着对象将始终保持与其父级相同的比例，无论其父级的大小如何 ）。

拉伸比例设为 `keep`（画面会被拉伸以填满整个视口，但不会变形。如果视口的宽高比例和游戏画面不同，会出现黑边）。

总之，只要能让我们的游戏画面更加清晰，怎么着都行。

