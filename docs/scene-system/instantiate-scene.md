# 如何实例化场景？

::: tip 目标

1. 该如何实例化场景？
2. 什么时候会用到“实例化场景”这种方式来扩展场景呢？

:::

<ImageTextCard>
<template #content>
跟着下面的指示一起做，说不定做着做着就学会了！

1. 新建一个名为 `Instantiate Scene` 的项目，;
2. 在默认场景下，创建一个 Node2D 类型的节点，作为该场景的根节点。然后保存该场景，并将其命名为 `main-sence.tscn`;

</template>
</ImageTextCard>
