<PageHeader content="如何创建一个 gdscript 脚本？" />

<VideoCard title="Bilibili 视频教程" downloadLink="/godot/zips/create_script.7z">
<template #content>
<video-iframe :video-id="834082629"></video-iframe>
</template>
</VideoCard>

<!--
- 视频脚本

1. 今天向大家分享 | “如何创建一个脚本”?
2. 脚本就是用来控制游戏节点行为的 | 简单讲，脚本就是能让我们的游戏动起来 | 这里我们使用 Godot 提供的脚本语言 GDScript 来创建脚本 | 并完成一个飞船移动的小例子

- 构建场景

1. 首先，打开 Godot，导入游戏素材，将文件夹重命名为 “sprites”。
2. 在创建游戏场景之前，我们需要对项目设置进行一些更改。打开控制面板……
2. 然后，选择 Node2D 节点，作为该场景的根节点 | 并将其重命名为 “main” | 接着，创建一个 Sprite 子节点，将其重命名为 “bg” | 将背景素材拖拽到纹理属性中 | 调节一下背景大小 | 如果，你是 Windows 系统的话，可以直接按住 Ctrl + S 键，保存该场景。
5. 接着，创建一个新的场景，选择 Sprite 节点，作为该场景的根节点，| 并将其重命名为 “palyer” | 然后，将飞船素材拖拽到纹理属性中 | 保存一下这个场景。
6. 接着，选择 main 主场景 | 再将 player 场景当为子节点，拖拽到主场景下 | 调整一下飞船的位置
7. 好了，我们的游戏场景就构建完毕，先运行一下 | 嗯！是我们想要的效果。| 那如何让飞船动起来呢？

- 创建脚本

1. 选中 player 场景，创建一个脚本。 | 如果脚本创建成功的话？ | 编辑器会自动地跳转到脚本编辑区域 | 并且节点处还会出现脚本标识
2. 接着，我们就可以在脚本编辑器中，写我们的逻辑代码了。
3. 删除无用的代码，保留 _process 函数 |  _process函数会在游戏画面每帧刷新的时候，调用一次 | 而 delta 参数表示自上一帧结束时到当前帧结束的时间间隔
4. 定义一个常量 speed 用来表示飞船的移动速度。
5. 在 _process 函数中，定义一个变量 direction 用来表示飞船的移动方向。【如果你不知道什么是向量，可以看一下这篇文章】
6. 然后，监听键盘方向键是否按下，来设置飞船移动方向。
6. 最后，通过改变 Sprite 对象上的 position 属性，进而来改变飞船的位置。【如果你这句代码的意思的话，可以看一下这篇文章】
7. 保存脚本，并运行游戏 | 嗯！飞船动起来了。Nice

然后，在游戏画面每一帧刷新的时候
不断地设置飞船对象的 position 位置属性，这样就能让我们的飞船就动起来了


- 结尾

好了，本期视频就到此结束了。如果你想要本视频演示的游戏素材的话！可以到视频评论区查看下载素材的网址。拜拜！

 -->
