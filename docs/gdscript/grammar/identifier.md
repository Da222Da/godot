<PageHeader content="标识符：" />

> 标识符在命名时需要遵循特定的规则: 如不能以数字开头，不能包含特殊字符等。

在编程语言中，标识符就是变量、函数、类的名称，如 `Player`、`player_name`、`player_health`。

```gdscript
extends Node

class_name Player

var player_name = "Alice"
var player_health = 100

```
